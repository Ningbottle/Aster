# Aster 开发约定

本文件只保存开发 Aster 时必须长期遵守的高代价约束。产品范围、架构说明、里程碑与估算以 [`content.md`](content.md) 为准。不要把 `content.md`、README 或研究报告复制到这里。

## 开始工作前

1. 先读 `content.md` 中与任务相关的部分，再查看真实代码、配置和测试。
2. 区分用户要求、已验证事实与临时假设。会改变产品边界、安全责任、数据可逆性或公开承诺的假设必须先确认；普通实现选择由代码、实验和短 ADR 决定。
3. 选择能形成可运行证据的最小纵向改动。不要用大批空接口、假数据页面或预建抽象冒充阶段完成。
4. 保护工作区中已有且与任务无关的修改；不要擅自重写、删除或格式化它们。

## 产品与架构不变量

- Alpha 唯一正式支持 Windows 10 22H2 / Windows 11 x64，普通用户权限。
- 技术主线是 Svelte 5 + TypeScript、Tauri 2、Rust Core、SQLite 与受监管子进程。
- Pi 与 DeepSeek Harness（DSH）是两个独立宿主。可以共享平台服务，但不共享 RPC 类型、对话状态机、插件模型或 UI 流程。
- Pi 只通过严格 JSONL RPC 连接。不要增加第二种 Pi 集成路线。
- DSH 优先保留其原生 Web UI、插件、自定义模式和持久化语义，不为统一外观重写其核心形态。
- `HostProfile` 是随 Aster 发布的版本化、只读、不可执行数据；运行时行为由内置、版本门控的 Rust connector 完成。
- SQLite 保存元数据、状态、关系、哈希和 evidence；大文件、宿主会话原文和不可变 Skill 内容保存在各自文件系统位置。
- EvidenceStore 是兼容与完成状态的事实源。文件存在、配置完成、宿主加载和真实可调用是不同证据阶段，不得合并成一个布尔“已兼容”。

## 账号、凭据与隐私

- Aster 没有账号域，不读取、保存、迁移或同步 Pi/DSH 的模型/API 凭据。
- 宿主配置缺失时只呈现状态和修复入口；不要把密钥复制进 Aster 的 SQLite、日志、导出包或诊断包。
- GitHub 私有来源的令牌只进入 Windows Credential Manager。
- Alpha 不上传遥测或崩溃报告。诊断包必须由用户主动生成、导出前可预览，并默认脱敏。
- 不记录对话原文、工作区文件内容、环境变量值、用户名或未经令牌化的绝对路径。

## Skills 安全与所有权

- 原始 Skill snapshot 不可变。中文 Markdown 说明是单独的派生文件，不能修改上游内容。
- Aster 可以下载、扫描、复制、部署和回滚 Skill，但绝不执行下载内容中的脚本或二进制。
- GitHub 是可选更新来源，不是 Skill 必须具备的身份；本地、离线和未知来源必须可表示。
- 部署前验证规范化路径、目标边界、重解析点/符号链接、文件类型、内容哈希和所有权。
- 默认复制，不默认建立 symlink/junction；绝不覆盖未托管目录。
- Aster 管理的目录被外部修改后，停止自动写入，展示差异并等待用户决定。
- 兼容证据至少绑定 Skill snapshot、目标宿主、宿主版本和部署作用域。Pi/DSH 可做深验证；其他宿主在 Alpha 只承诺文件与静态证据。

## 安装、更新与恢复

- Aster、Pi、DSH 更新是三个独立任务。界面中的“全部更新”只表示排队，不表示跨产品原子事务。
- 可以自动检查，不能静默安装；任何安装或切换都需要用户明确确认。
- 只直接更新 Aster 管理的安装，或能够高置信识别且官方更新方式明确的安装。未知、外部管理或手工修改的安装不得覆盖。
- Aster 管理的 Pi/DSH 使用并排程序版本、验证后切换活动版本，并保留上一可用程序版本。
- “回滚”只保证 Aster 管理的程序文件；宿主数据、插件状态或迁移可能不可逆，必须单独显示兼容性为已知或未知。
- 遇到未知/不支持的宿主版本时安全降级：保留发现、启动、停止、原生界面和快照等安全能力，暂停不兼容的深层控制并标记“未验证”。

## Skills 使用政策

- 禁止把 Superpowers 的完整流程设为项目默认工作流，也不要把它写入计划、检查清单或完成门禁。
- 用户点名或任务明确需要时，可以使用单个、聚焦的 Skill。Skill 提供方法，不替代源码检查、工程判断和验证证据。
- 不为“遵循流程”而叠加多个无关 Skill；能由 formatter、linter、类型系统、schema 或测试执行的规则，不重复写成长提示词。

## 实现自由与可读性

- 本文件不规定固定目录树、trait 名、表结构、组件拆分或设计模式。选择能满足责任边界、错误处理和测试性的最简单实现。
- 函数约 60–80 行、手写源文件约 400–600 行时应主动检查是否混合职责；这只是审查信号，不是失败门槛。生成文件、vendor、迁移和测试数据不适用。
- 优先减少耦合、嵌套、隐式状态和跨层写操作。名称表达领域含义；注释解释原因、约束和异常情况，不复述代码。
- 外部协议、文件系统和 SQLite 边界使用明确类型与可诊断错误。不要吞错，不把未知状态伪装成成功。
- 不提前为未出现的第三个宿主或远程 connector 设计通用框架；出现第二个真实重复并证明稳定后再抽取。

## 验证要求

- 测试与风险成比例，并覆盖成功、取消、超时、崩溃、部分写入、校验失败、外部修改和恢复路径。
- 协议与上游版本使用固定 fixture/contract test；CI 不依赖上游仓库的实时状态。前端 invoke 参数与后端 command 结构之间同样要有契约测试（前端序列化样本 → Rust serde round-trip）：M3 的前端发 `host_id` 后端收 `host` 的断裂在全部绿灯下无人发现（ADR-0005）。
- selftest 与单元测试绿灯不构成“完成”。用户可见功能的完成必须附真实 UI 链路（UI→invoke→后端）的操作证据：M1–M3 的后端内部自检全绿而 UI 功能不可用。
- 前端不硬编码业务或演示数据（技能、证据、会话、计数、状态标签），由 CI grep 门禁执行；后端错误必须在 UI 可见，禁止空 catch 吞错。假数据 UI 比没有 UI 更糟：它让用户和开发者都误以为功能可用。
- Windows 文件操作至少考虑长路径、大小写、ACL/只读、文件锁、杀毒占用、junction/symlink/reparse point、ADS 和跨卷行为。
- 没有运行验证就不要声称“通过”“兼容”“可回滚”或“完成”。报告执行的命令、关键结果和未验证项。
- 构建、测试与验证命令以 README 与 CI 中的唯一权威命令为准；不要虚构无法在仓库中运行的命令。

## 完成定义

一项改动只有在以下条件满足时才算完成：

- 用户可见结果与 `content.md` 的范围一致；
- 相关测试或可重复检查通过，失败路径有证据；用户可见功能已经真实 UI 链路操作验证；
- 数据写入可追踪，恢复边界真实且没有扩大承诺；
- 没有泄漏凭据或静默覆盖用户管理的数据；
- 架构/产品边界变化已更新 `content.md`，高代价实现决定已写短 ADR；
- 已明确记录仍然未知、只做静态验证或因版本而降级的能力。

## 文档维护

- 新增长期规则时，先说明它防止的真实失败，并优先替换或合并旧规则，避免只增不减。
- 临时任务步骤进入 issue/计划，产品状态进入 `content.md`，设计权衡进入 ADR，命令进入 README/CI；不要堆入本文件。
- 每个 Alpha 里程碑结束时审查本文件，删除已经由代码或工具强制、过时或重复的自然语言规则。

